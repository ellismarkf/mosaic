#user inputs text, presses enter

module Transliterator
  class TextCapture
    def self.run(params)
      #run capture_text.rb, which splits text into a hash  (input.scan)
      #store hash


    end
  end
end

# get '/colors' do
#   @text_hash = TextCapture.run
#   erb :view
# end